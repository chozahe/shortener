<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Ошибка</title>
</head>

<body>
<h1>Что-то пошло не так</h1>

<p>{{ status_code }} {{ status_text }}</p>

{% if exception is defined and app.debug %}
    <hr>
    <pre>{{ exception.message }}</pre>
{% else %}
    <p>Попробуйте вернуться на <a href="{{ path('homepage') }}">главную</a>.</p>
{% endif %}
</body>
</html>
