<head>
    <meta charset="UTF-8">
    <title>Сократить ссылку!!</title>
    <link rel = "stylesheet" href="{{ asset('styles/style.css') }}">
</head>

{{ form_start(form) }}
        {{ form_errors(form) }}

<div class="form-group">
    {{ form_label(form.originalUrl) }}
    {{ form_widget(form.originalUrl) }}
    {{ form_errors(form.originalUrl) }}
</div>

<div class="form-group">
    {{ form_label(form.isDisposable) }}
    {{ form_widget(form.isDisposable) }}
    {{ form_errors(form.isDisposable) }}
</div>

<div class="form-group">
    {{ form_label(form.expiresAt) }}
    {{ form_widget(form.expiresAt) }}
    {{ form_errors(form.expiresAt) }}
</div>

<button type="submit">Создать</button>
{{ form_end(form) }}

{% if short_url is defined and short_url %}
    <p>Твоя короткая ссылка: <a href="{{ short_url }}">{{ short_url }}</a></p>
{% endif %}

<a href="/list">Статистика!</a>


